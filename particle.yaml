# This config file is solely for Grav to provide theme inheritance.
# Our base Grav theme is `particle` which inherits the compiled version as its source.
streams:
 schemes:
   theme:
     type: ReadOnlyStream
     prefixes:
       '':
         - user/themes/particle
         - user/themes/particle/apps/grav
