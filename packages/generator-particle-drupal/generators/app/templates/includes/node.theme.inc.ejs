<?php

/**
 * @file
 * Functions to support theming node entities in the <%= themeNamePascal %> theme.
 */

/**
 * Implements hook_preprocess_node().
 */
function <%= themeNameSnake %>_preprocess_node(array &$variables) {
  // Default to turning off byline/submitted.
  $variables['display_submitted'] = FALSE;
}
