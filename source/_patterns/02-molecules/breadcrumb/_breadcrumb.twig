{#
  Breadcrumb!

  breadcrumb_items: Array: A list of breadcrumbs to render.
  breadcrumb_title: String: The title of the breadcrumb
  breadcrumb_link: String: The URL of the breadcrumb

  @TODO: Add ability to toggle 'active' class and aria-current attribute to page.

  see https://www.w3.org/TR/wai-aria-practices/#breadcrumb

  see https://getbootstrap.com/docs/4.0/components/breadcrumb/ for more details
#}

<nav aria-label="breadcrumb" role="navigation">
  <ol class="breadcrumb">
    {% if breadcrumb_items %}
      {% for item in breadcrumb_items %}
        <li class="breadcrumb-item"><a href="{{ item.breadcrumb_link }}">{{ item.breadcrumb_title }}</a></li>
      {% endfor %}
    {% endif %}
  </ol>
</nav>
