/**
 * Breadcrumb
 */

// Module dependencies
import base from 'base';

// Module styles
import './_breadcrumb.scss';

export default {
  name: 'breadcrumb',
  deps: [base],
  enable() {},
  disable() {},
};
