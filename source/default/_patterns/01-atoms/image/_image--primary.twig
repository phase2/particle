{#
/**
 * @file
 * image--primary
 *   Implements images src and alt.
 *
 * Available variables for image:
 *  - Inherit variables from @atoms/_image.twig
 */
#}

<img src="{{ image.src }}" alt="{{ image.alt }}" class="{{ image_classes }}" />
