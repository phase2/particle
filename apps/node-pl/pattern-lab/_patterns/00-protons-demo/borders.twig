{#
/**
 * @file Borders!
 * Documenting Border Examples from Bootstrap. These are Bootstrap Utilities we gain via Base.
 *
 * Available variables:
 * - border_classes: [array] Used to demonstrate available border utility classes.
 * - radius_classes: [array] Used to demonstrate available boder-radius utility classes.
 *
 * @see https://getbootstrap.com/docs/4.0/utilities/borders/
 */
#}

{% set border_classes = [
  '',
  '-0',
  '-top-0',
  '-right-0',
  '-bottom-0',
  '-left-0',
] %}

{% set radius_classes = [
  '',
  '-top',
  '-right',
  '-bottom',
  '-left',
  '-circle',
  '-0',
] %}

<div class="container">
  <div class="row">
    <div class="col">
      <h1 class="mb-3">Borders</h1>
      <div class="mb-3 p-3 bg-light border border-secondary rounded">
        <p class="lead">Use border utilities to quickly style the border and
          border-radius of an element. Great for images, buttons, or any other
          element.
        </p>
        <div><a href="https://getbootstrap.com/docs/4.0/utilities/borders/" target="_blank" rel="noopener noreferrer">Bootstrap Documentation</a></div>
      </div>
      <hr>

      <h2 class="py-3">Border</h2>
      <p>Add classes to an element to remove all borders or some borders.</p>
      <div class="border">
        {% for class in border_classes %}
          <span class=" bg-light m-2 border border{{ class }}"
                style="display: inline-block; height: 5rem; width: 5rem; margin: .25rem;"></span>
        {% endfor %}
        <pre class="bg-light mb-0">
          <code>
{% for class in border_classes %}
  &lt;span class="border{{ class }}"&gt;&lt;/span&gt;
{% endfor %}
          </code></pre>
      </div>

      <h2 class="py-3">Border Color</h2>
      <p>Change the border color using utilities built on our theme colors.</p>
      <div class="border">
        {% for colorName, colorValue in scssColors %}
          <span class="bg-light m-2 border border-{{ colorName }}"
                style="display: inline-block; height: 5rem; width: 5rem; margin: .25rem;"></span>
        {% endfor %}

        <pre class="bg-light mb-0">
          <code>
{% for colorName, colorValue in scssColors %}
  &lt;span class="border border-{{ colorName }}"&gt;&lt;/span&gt;
{% endfor %}
          </code></pre>
      </div>

      <h2 class="py-3">Border-radius</h2>
      <p>Add classes to an element to easily round its corners.</p>
      <div class="border">
        {% for class in radius_classes %}
          {% include '@atoms/image/_image.twig' with {
            image: border_radius_image,
            image_classes: ['m-2 rounded' ~ class],
          } %}
        {% endfor %}
        <pre class="bg-light mb-0">
          <code>
{% for class in radius_classes %}
  &lt;img src="..." alt="..." class="rounded{{ class }}"&gt;
{% endfor %}
          </code></pre>
      </div>

    </div>
  </div>
</div>
